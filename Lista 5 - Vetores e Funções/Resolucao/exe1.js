// vetor vazio
let vetor = [];

// Loop para preencher o vetor com o cubo do índice
for (let i = 0; i < 8; i++) {
    vetor[i] = i ** 3; // Calcular o cubo do índice
}

// Resultado do vetor
console.log(vetor);
